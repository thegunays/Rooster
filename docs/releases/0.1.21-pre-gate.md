# Rooster Description Editor 0.1.21 pre-gate test artifact

Date: 2026-08-20

## Status and boundary

This document records fresh local results for one private pre-gate test artifact. It does not record an installation, publication, hosted-CI run, Azure DevOps request, or real Work Item mutation. Both required Azure-host scenarios are `pending external approval and execution`.

## Preserved behavior and approved hardening

The automated and local harness evidence preserves editable, unsupported-WIT, read-only, configured-field, autosync, echo, save/refresh/reset/unload/reload, toolbar, table-menu, BKU table/style, and field-isolation behavior. The approved intentional hardening includes DOMPurify-backed HTML sanitization, css-tree-backed scoped CSS filtering, a canonical content root, blocked implicit `System.Description` writes, retained autosync failure state until an explicit superseding event, logical table-selection targeting and aggregate checked states, transactional write recovery/save flush and package verification, bounded content-safe errors, runtime telemetry validation, and accessibility semantics without visual-order changes.

## Fresh local gates

| Gate | Fresh result |
| --- | --- |
| `npm ci` | Exit 0; 511 packages installed from the lockfile. |
| `npm run typecheck` | Exit 0; no diagnostics. |
| `npm test -- --reporter=verbose` | Exit 0; 27 files, 656 passed, 0 failed, 0 skipped. |
| `npm run build` | Exit 0; production build completed with the three known Webpack size/entrypoint/performance warnings. |
| `npm run check:build-outputs -- production` | Exit 0; exact production listing passed. |
| `npm run build:harness` | Exit 0. |
| `npm run check:build-outputs -- harness` | Exit 0; exact harness listing passed. |
| `npm run audit:prod` | Exit 0; 0 production vulnerabilities. |
| `npm run package:vsix` | Exit 0; local build, release check, pinned local packaging, and VSIX verification passed. |

The production `dist/control.js` is 778,286 bytes. The prior baseline was 537,439 bytes: an increase of 240,847 bytes, or 44.81%. The increase reflects the rewrite's bundled DOMPurify, css-tree, and host/controller code; Webpack's size warnings remain visible rather than suppressed.

## Exact artifact and version alignment

- Filename: `ygdb121.roosterjs-description-editor-0.1.21.vsix`
- Location: ignored `artifacts/` only; it is the sole artifact there.
- Byte size: 224,647
- SHA-256: `6626fa840f9f86a22db6e8147263b4359732a3faef5607cd15ed02be3b57ad04`
- Identity: `ygdb121.roosterjs-description-editor`
- Scope: sole `vso.work_write`
- Version: `0.1.21` in `vss-extension.json`, `package.json`, both root lock mirrors, and the effective `extension.vsixmanifest` identity. The generated `extension.vsomanifest` has no separate release-version field; its manifest version, scope, contribution, properties, and five inputs match the source contract.
- Packaging tool: repository-local, lock-resolved `tfx-cli@0.23.1`, invoked through the current Node executable without a shell.

The verifier accepted exactly these sorted raw entries:

```text
[Content_Types].xml
dist/
dist/control.js
dist/control.js.LICENSE.txt
extension.vsixmanifest
extension.vsomanifest
static/
static/control.css
static/control.html
```

The raw ZIP structure, entry types and names, effective XML/JSON identity and scope, exact five-input contribution, and four packaged repository payloads passed independent checks. Harness bundles, source, tests, fixtures, docs, `node_modules`, maps, secrets, junk, and platform binaries are absent.

The complete-file SHA above identifies this exact pre-gate test artifact. Repacking can change ZIP timestamps, so a future package run must record and verify its own SHA and size.

## Upgrade procedure after explicit approval

1. Retain the last known private VSIX and its installation details for rollback.
2. Reconfirm the exact filename, SHA-256, version alignment, and verifier result above.
3. Install or update only this pre-gate test artifact in the approved private, non-production Azure DevOps organization/collection.
4. Execute both scenarios in `docs/regression/azure-devops-regression.md` and retain the required raw-field, screenshot, write-log, DOM/style, and console evidence.
5. Treat any failed or unverifiable observation as a blocked gate.

No data migration is required.

## Rollback

Disable or uninstall the pre-gate test artifact, then reinstall the last known private VSIX. No data migration or stored-field rewrite is required. Preserve captured diagnostics and stop further host mutation until the failure is understood.

## External evidence

Real Azure evidence status: `pending external approval and execution`.
